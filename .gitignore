# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
supabase/.temp/

# Local Netlify folder
.netlify

# Apple Wallet private key — never commit this.
# The public certs (pass_cert.pem, wwdr.pem) ARE committed and bundled with
# the Lambda function.  Only the private key must stay out of git.
netlify/functions/certs/*.key.pem
netlify/functions/certs/key.pem

# Sentry Config File
.env.sentry-build-plugin
